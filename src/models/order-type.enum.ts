export enum OrderType {
  STORE_DROPOFF = 'store_dropoff',       // customer drops at store, picks up from store
  STORE_DROPOFF_HOME_DELIVERY = 'store_dropoff_home_delivery', // drops at store, delivered home
  EXPRESS = 'express',
}
